<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Горизонт меню
    |--------------------------------------------------------------------------
    |
    | На скільки днів уперед учень бачить меню (ТЗ, п. 17.4 — 14 днів).
    |
    */

    'menu_horizon_days' => (int) env('SCHOOL_MENU_HORIZON_DAYS', 14),

    /*
    |--------------------------------------------------------------------------
    | Номер замовлення
    |--------------------------------------------------------------------------
    |
    | Один номер на весь чек, навіть якщо в ньому кілька постачальників і дат.
    |
    */

    'order_number_prefix' => env('SCHOOL_ORDER_NUMBER_PREFIX', 'ЗМ'),

    /*
    |--------------------------------------------------------------------------
    | Токен обслуговування
    |--------------------------------------------------------------------------
    |
    | Потрібен лише на хостингу без SSH: дозволяє запускати міграції та
    | перебудову кешу через браузер. Поки значення порожнє, маршрут
    | /maintenance не існує зовсім — після запуску сайту токен варто прибрати.
    |
    */

    'maintenance_token' => env('MAINTENANCE_TOKEN'),

    /*
    |--------------------------------------------------------------------------
    | Токен планувальника
    |--------------------------------------------------------------------------
    |
    | Заміна cron на хостингах, де планувальник уміє лише «смикати URL».
    |
    */

    'scheduler_token' => env('SCHEDULER_TOKEN'),

    /*
    |--------------------------------------------------------------------------
    | Публічна папка
    |--------------------------------------------------------------------------
    |
    | Потрібно лише там, де document root змінити не можна і публічна папка
    | лежить поза проєктом — наприклад ~/public_html, тоді як код у
    | ~/school-food. Порожньо — використовується public_html усередині проєкту.
    |
    | Від цього значення залежить, куди підуть storage:link і асети Filament.
    |
    */

    'public_path' => env('APP_PUBLIC_PATH'),

];
